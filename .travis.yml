language: python

jobs:
  include:
    - os: linux
    - os: osx
      language: generic
      
python:
  - "3.7"
  - "3.8"

install:
  - pip install --upgrade pip
  - pip install -r requirements.txt

script:
  - pytest -W ignore::DeprecationWarning